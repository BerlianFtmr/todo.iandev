# Membuat DESIGN.md

kondisi :
saat ini aplikasi ini sudah bisa berjalan dengan baik, dan sudah di testing.

rencana :
Dari design UI aplikasi yang sudah ada, saya ingin kamu membuat sebuah file DESIGN.md, file tersebut harus mendokumentasikan design dan visual pattern yang saat ini sudah digunakan oleh aplikasi. Tujuannya adalah agar DESIGN.md dapat menjadi referensi untuk memahami dan mempertahankan konsistensi design aplikasi di pengembangan berikutnya.

---

# Mengganti nama aplikasi

kondisi :
saat ini aplikasi sudah berjalan dengan baik, datanya juga sudah tidak tersimpan secara temporary, design patern nya juga sudah sesuai.

masalah :
untuk nama aplikasi belum sesuai dengan apa yang aku inginkan, coba kamu ubah nama aplikasi ini menjadi todo.iandev

---

# production build

kondisi :
saat ini aplikasi sudah sesuai yang di inginkan untuk frontend, backend maupun database, backend aku menggunakan express.js dan database aku menggunakan mysql dalam docker, namun saat ini untuk menjalankan aplikasi masih menggunakan command `npm run dev`.

rencana :
aku ingin kamu membuatkan dockerfile untuk production untuk aplikasi express.js + ejs dan docker-compose.yml untuk production, agar aplikasi bisa berjalan dalam docker production, untuk jaringan, reverse proxy,atau ssl sudah di handle oleh cloudflare, buat konfigurasi agar aplikasi bisa berjalan dalam docker build

---

# Menambah fitur pilih warna dalam add folder

kondisi :
saat ini aplikasi sudah berjalan sesuai yang di inginkan, CRUD folder dan task sudah berjalan dengan baik

rencana :
aku ingin menambahkan fitur pilihan warna pada saat create folder, untuk fitur pilihan warna agar lebih fleksibel, user akan memasukkan warna menggunakan kode hex warnanya.

tidak perlu menggunakan color picker, nanti user langsung memasukkan code hex saja dan nanti warnanya berganti sesuai dengan code hex yang di inputkan

aku sudah melakukan test untuk fitur warnanya sekarang aku ingin, setiap perubahan warna nantinya card task dari folder yang di costum warna pattern nya akan mengikuti warna yang sudah di pilih user
